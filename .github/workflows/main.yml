name: Bioinformatics Journey

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Get repository
        uses: actions/checkout@v4

      - name: Say hello
        run: echo "My bioinformatics journey is running!"
